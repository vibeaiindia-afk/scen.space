import {NextRequest,NextResponse} from 'next/server';import {requireAdmin} from '@/lib/admin-auth';import {billingStatus} from '@/lib/billing/config';import {dbHealth,db} from '@/lib/data/db';export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{requireAdmin(req);const providers=billingStatus();let database:any={ok:false};try{database=await dbHealth()}catch{}return NextResponse.json({providers,database,webhooks:{stripe:'/api/billing/webhooks/stripe',razorpay:'/api/billing/webhooks/razorpay',dodo:'/api/billing/webhooks/dodo'},creditEngine:'database-ledger'})}catch(e:any){return NextResponse.json({error:e?.message||'Admin billing status failed'},{status:e?.status||500})}}
