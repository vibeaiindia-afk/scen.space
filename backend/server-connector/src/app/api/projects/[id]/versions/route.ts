import type { NextRequest } from 'next/server';import { requireUser } from '../../../../../lib/user-auth';import { createVersion,listVersions } from '../../../../../lib/data/versions';
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;return Response.json({versions:await listVersions(await requireUser(req),id)})}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,v=await createVersion(await requireUser(req),id,await req.json().catch(()=>({})));return v?Response.json({version:v},{status:201}):Response.json({error:'Project not found'},{status:404})}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
