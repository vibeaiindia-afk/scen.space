import type { UserSession } from '../user-auth';import { assetKey } from './keys';import { putBytes } from './s3';import { registerAsset } from '../data/assets';
export async function persistGeneratedBytes(s:Pick<UserSession,'sub'|'workspaceId'>,input:{bytes:Uint8Array;contentType:string;filename:string;kind:string;provider?:string;model?:string;projectId?:string;metadata?:any}){const key=assetKey(s.workspaceId,input.filename);const stored=await putBytes(key,input.bytes,input.contentType);const asset=await registerAsset(s,{projectId:input.projectId,kind:input.kind,key,mimeType:input.contentType,bytes:stored.bytes,originalName:input.filename,provider:input.provider,model:input.model,metadata:input.metadata});return {assetId:asset.id,url:`/api/assets/${asset.id}/content`,key}}
