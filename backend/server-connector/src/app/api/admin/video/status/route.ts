import { NextRequest,NextResponse } from 'next/server';import { requireAdmin } from '@/lib/admin-auth';import { videoProviders } from '@/lib/video/registry';import { videoRoute,videoLimits } from '@/lib/video/config';
export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{requireAdmin(req);return NextResponse.json({providers:videoProviders().map(p=>({id:p.id,envKey:p.envKey,configured:p.isConfigured(),defaultModel:p.defaultModel})),routing:videoRoute(),limits:videoLimits(),deployEnabled:false},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Unauthorized'},{status:e?.status||401})}}
