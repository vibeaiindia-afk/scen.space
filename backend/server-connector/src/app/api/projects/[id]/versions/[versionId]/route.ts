import type { NextRequest } from 'next/server';import { requireUser } from '../../../../../../lib/user-auth';import { getVersion } from '../../../../../../lib/data/versions';
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string;versionId:string}>}){try{const {id,versionId}=await params,v=await getVersion(await requireUser(req),id,versionId);return v?Response.json({version:v},{headers:{'Cache-Control':'no-store'}}):Response.json({error:'Version not found'},{status:404})}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
