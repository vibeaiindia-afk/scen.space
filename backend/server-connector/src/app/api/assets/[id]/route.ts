import type { NextRequest } from 'next/server';import { requireUser } from '../../../../lib/user-auth';import { trashAsset } from '../../../../lib/data/assets';export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,a=await trashAsset(await requireUser(req),id);return a?Response.json({ok:true,softDeleted:true}):Response.json({error:'Asset not found'},{status:404})}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
