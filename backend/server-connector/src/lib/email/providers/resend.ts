export async function sendResend(x:{from:string;to:string;subject:string;html:string;text?:string;replyTo?:string;idempotencyKey:string}){const key=process.env.RESEND_API_KEY;if(!key)throw Object.assign(new Error('RESEND_API_KEY is not configured'),{status:503});const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','User-Agent':'Scen/1.0','Idempotency-Key':x.idempotencyKey},body:JSON.stringify({from:x.from,to:[x.to],subject:x.subject,html:x.html,text:x.text,reply_to:x.replyTo}),cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(j.message||'Resend send failed'),{status:r.status});return{id:String(j.id||''),provider:'resend'}}
