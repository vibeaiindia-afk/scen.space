import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {db} from '@/lib/data/db';export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{const s=await requireUser(req),sql=db();const rows=await sql`select id,provider,provider_invoice_id,amount_minor,currency,status,hosted_url,pdf_url,issued_at from billing_invoices where workspace_id=${s.workspaceId} order by issued_at desc nulls last,created_at desc limit 100`;return NextResponse.json({invoices:rows},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Invoices unavailable'},{status:e?.status||500})}}
