import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {listSessions,revokeOthers} from '@/lib/auth/session';export async function GET(req:NextRequest){try{const s=await requireUser(req);return NextResponse.json({sessions:await listSessions(s)},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Unauthorized'},{status:e?.status||401})}}export async function DELETE(req:NextRequest){try{const s=await requireUser(req);if(new URL(req.url).searchParams.get('others')!=='true')return NextResponse.json({error:'Use a session-specific revoke route or others=true'},{status:400});return NextResponse.json({ok:true,revoked:await revokeOthers(s)})}catch(e:any){return NextResponse.json({error:e?.message||'Unauthorized'},{status:e?.status||401})}}