import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {requestVerify} from '@/lib/auth/flows';export async function POST(req:NextRequest){try{const s=await requireUser(req);if(!s.email)return NextResponse.json({error:'Account email missing'},{status:409});await requestVerify(s.sub,s.email);return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e?.message||'Verification request failed'},{status:e?.status||500})}}