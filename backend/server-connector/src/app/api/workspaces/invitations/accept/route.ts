import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {acceptInvite} from '@/lib/auth/flows';export async function POST(req:NextRequest){try{const s=await requireUser(req),b=await req.json();return NextResponse.json({membership:await acceptInvite(s,String(b?.token||''))})}catch(e:any){return NextResponse.json({error:e?.message||'Invite acceptance failed'},{status:e?.status||400})}}