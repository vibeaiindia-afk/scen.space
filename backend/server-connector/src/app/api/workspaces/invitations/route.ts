import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {createInvite} from '@/lib/auth/flows';export async function POST(req:NextRequest){try{const s=await requireUser(req),b=await req.json();return NextResponse.json({invitation:await createInvite(s,String(b?.email||''),String(b?.role||'member'))},{status:201})}catch(e:any){return NextResponse.json({error:e?.message||'Invite failed'},{status:e?.status||400})}}