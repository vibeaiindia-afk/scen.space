import { NextRequest,NextResponse } from 'next/server';import { requireAdmin } from '@/lib/admin-auth';import { videoProvider } from '@/lib/video/registry';import type { VideoProviderId } from '@/lib/video/types';
export const dynamic='force-dynamic';const ids:VideoProviderId[]=['openai','gemini','replicate'];export async function POST(req:NextRequest){try{requireAdmin(req);const b=await req.json(),id=String(b?.provider||'') as VideoProviderId;if(!ids.includes(id))return NextResponse.json({error:'Unknown video provider'},{status:400});const r=await videoProvider(id).test(String(b?.model||'')||undefined);return NextResponse.json({...r,provider:id,paidGeneration:false},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Provider test failed',code:e?.code},{status:e?.status||500,headers:{'Cache-Control':'no-store'}})}}
