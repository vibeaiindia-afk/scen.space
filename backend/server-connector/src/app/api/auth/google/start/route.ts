import {NextRequest,NextResponse} from 'next/server';import {beginGoogle} from '@/lib/auth/google';export async function GET(req:NextRequest){try{const {url,state}=await beginGoogle(new URL(req.url).searchParams.get('returnTo')||'/');const res=NextResponse.redirect(url);res.cookies.set('scen_google_oauth_state',state,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});return res}catch(e:any){return NextResponse.json({error:e?.message||'Google OAuth unavailable'},{status:e?.status||500})}}