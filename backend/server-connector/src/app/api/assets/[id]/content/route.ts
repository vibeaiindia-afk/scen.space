import type { NextRequest } from 'next/server';import { requireUser } from '../../../../../lib/user-auth';import { getAsset } from '../../../../../lib/data/assets';import { signGet } from '../../../../../lib/storage/s3';
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,a=await getAsset(await requireUser(req),id);if(!a)return Response.json({error:'Asset not found'},{status:404});return Response.redirect(await signGet(a.object_key,a.original_name||undefined),307)}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
