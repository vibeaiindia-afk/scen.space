import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';import {wallet,ledger} from '@/lib/billing/credits';export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{const s=await requireUser(req);const [w,l]=await Promise.all([wallet(s),ledger(s,50)]);return NextResponse.json({wallet:{available:Number(w.available),reserved:Number(w.reserved),lifetimePurchased:Number(w.lifetime_granted),lifetimeUsed:Number(w.lifetime_used)},ledger:l},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Credits unavailable'},{status:e?.status||500})}}
