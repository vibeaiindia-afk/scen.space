import {NextRequest,NextResponse} from 'next/server';import {requireUser} from '@/lib/user-auth';export async function GET(req:NextRequest){try{const s=await requireUser(req);return NextResponse.json({user:{id:s.sub,email:s.email,emailVerified:s.emailVerified},workspaceId:s.workspaceId,role:s.role,plan:s.plan||null},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return NextResponse.json({error:e?.message||'Unauthorized'},{status:e?.status||401})}}