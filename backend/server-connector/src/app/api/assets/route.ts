import type { NextRequest } from 'next/server';import { requireUser } from '../../../lib/user-auth';import { listAssets } from '../../../lib/data/assets';export async function GET(req:NextRequest){try{return Response.json({assets:await listAssets(await requireUser(req),Number(new URL(req.url).searchParams.get('limit')||100))})}catch(e:any){return Response.json({error:String(e?.message||e)},{status:e?.status||500})}}
