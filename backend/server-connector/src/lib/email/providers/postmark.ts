export async function sendPostmark(x:{from:string;to:string;subject:string;html:string;text?:string;replyTo?:string;tag?:string}){const key=process.env.POSTMARK_SERVER_TOKEN;if(!key)throw Object.assign(new Error('POSTMARK_SERVER_TOKEN is not configured'),{status:503});const r=await fetch('https://api.postmarkapp.com/email',{method:'POST',headers:{'X-Postmark-Server-Token':key,'Accept':'application/json','Content-Type':'application/json'},body:JSON.stringify({From:x.from,To:x.to,Subject:x.subject,HtmlBody:x.html,TextBody:x.text,ReplyTo:x.replyTo,Tag:x.tag,MessageStream:'outbound'}),cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok||Number(j.ErrorCode||0)!==0)throw Object.assign(new Error(j.Message||'Postmark send failed'),{status:r.status||502});return{id:String(j.MessageID||''),provider:'postmark'}}
