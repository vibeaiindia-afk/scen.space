import { VideoProviderError } from './types';
export async function moderateVideoPrompt(prompt:string,signal:AbortSignal){if(process.env.SCEN_VIDEO_PROMPT_MODERATION==='off'||!process.env.OPENAI_API_KEY)return;const r=await fetch('https://api.openai.com/v1/moderations',{method:'POST',signal,headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.SCEN_MODERATION_MODEL||'omni-moderation-latest',input:prompt})});if(!r.ok)return;const body=await r.json();if(body?.results?.[0]?.flagged)throw new VideoProviderError('openai','Prompt blocked by moderation',400,false,'moderation_block')}
