import {NextRequest,NextResponse} from 'next/server';import {requireAdmin} from '@/lib/admin-auth';import {emailStatus} from '@/lib/email/config';export async function POST(req:NextRequest){try{requireAdmin(req);const b=await req.json().catch(()=>({})),s=emailStatus();if(b?.mode==='send'&&process.env.SCEN_EMAIL_TEST_TO)return NextResponse.json({configured:s.configured,provider:s.provider,note:'Use a dedicated operator test route only after explicit recipient confirmation.'});return NextResponse.json(s)}catch(e:any){return NextResponse.json({error:e?.message||'Admin required'},{status:e?.status||401})}}